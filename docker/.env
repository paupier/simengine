# simengine Docker Stack - Default Environment Variables
# Override these in Portainer or docker compose. The compose files already
# ship the same values as `${VAR:-default}` fallbacks, so this file only
# matters once you actually change something below.

INFLUXDB_TOKEN=simengine-dev-token
NEO4J_PASSWORD=simengine-dev
